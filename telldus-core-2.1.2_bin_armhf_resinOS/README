This will install telldus core on the homeassistant docker for experimenting
tellstick.conf will be editable in /config share.

It will not commit to the docker so after reboot the docker will be reset (and tellstick core removed)
